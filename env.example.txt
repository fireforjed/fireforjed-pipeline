# Copy this file to .env for local testing. Never commit the real .env file.

FB_PAGE_ID=
FB_PAGE_ACCESS_TOKEN=
IG_USER_ID=
IG_ACCESS_TOKEN=

# Only needed if you're testing publish_post.py directly, outside the
# GitHub Actions workflow. IMAGE_URL must already be a publicly
# reachable URL (e.g. a raw.githubusercontent.com link to an image
# that's already been pushed).
IMAGE_URL=
CAPTION=
